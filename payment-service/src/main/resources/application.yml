server:
  port: 8081
  servlet:
    context-path: /payment-service/v1
spring:
  application:
    name: payment-service
  datasource:
    name: payment-service
    url: jdbc:mysql://localhost:3306/payment_db
    username: shiv
    password: shiv
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect